<div class="rounded-[1.4rem] border border-white/10 bg-black/20 p-4">
    <div class="flex flex-wrap items-start justify-between gap-3">
        <div>
            <div class="flex flex-wrap items-center gap-2">
                <p class="text-sm font-semibold text-white">@Title</p>
                @if (OnHelpClick.HasDelegate)
                {
                    <button type="button"
                            @onclick="OnHelpClick"
                            class="inline-flex h-7 w-7 items-center justify-center rounded-full border border-white/10 bg-white/5 text-xs font-semibold text-neutral-200 transition hover:bg-white/10">
                        ?
                    </button>
                }
            </div>
            <p class="mt-1 text-xs font-medium uppercase tracking-[0.2em] text-fuchsia-200/80">@Variable</p>
        </div>

        <div class="flex flex-wrap gap-2">
            <StatusTag Text="@AvailabilityLabel" />
            <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-[0.68rem] font-semibold uppercase tracking-[0.2em] text-neutral-300">
                @ValueType
            </span>
        </div>
    </div>

    <div class="mt-4">
        <label class="mb-2 block text-sm font-medium text-neutral-300">@InputLabel</label>
        <select value="@Value"
                @onchange="OnChanged"
                class="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3 text-white outline-none">
            @foreach (string option in Options)
            {
                <option value="@option">@option</option>
            }
        </select>
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public string Title { get; set; } = "";

    [Parameter, EditorRequired]
    public string Variable { get; set; } = "";

    [Parameter, EditorRequired]
    public string Description { get; set; } = "";

    [Parameter, EditorRequired]
    public string ValueType { get; set; } = "";

    [Parameter, EditorRequired]
    public string AvailabilityLabel { get; set; } = "";

    [Parameter]
    public string InputLabel { get; set; } = "Value";

    [Parameter]
    public IReadOnlyList<string> Options { get; set; } = Array.Empty<string>();

    [Parameter]
    public string? Value { get; set; }

    [Parameter]
    public EventCallback<string?> ValueChanged { get; set; }

    [Parameter]
    public EventCallback OnHelpClick { get; set; }

    private Task OnChanged(ChangeEventArgs eventArgs)
    {
        Value = eventArgs.Value?.ToString();
        return ValueChanged.InvokeAsync(Value);
    }
}
